name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  build:
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            npm-dir: linux-x64-gnu
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            npm-dir: linux-arm64-gnu
            use-cross: true
          - target: x86_64-apple-darwin
            os: macos-latest
            npm-dir: darwin-x64
          - target: aarch64-apple-darwin
            os: macos-latest
            npm-dir: darwin-arm64

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross
        if: matrix.use-cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build
        run: |
          if [ "${{ matrix.use-cross }}" = "true" ]; then
            cross build --release --target ${{ matrix.target }}
          else
            cargo build --release --target ${{ matrix.target }}
          fi

      - name: Copy binary to npm package
        run: |
          cp target/${{ matrix.target }}/release/omz2cc npm/${{ matrix.npm-dir }}/omz2cc
          chmod +x npm/${{ matrix.npm-dir }}/omz2cc

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.npm-dir }}
          path: npm/${{ matrix.npm-dir }}/

  publish:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          # Do NOT use registry-url — it injects NODE_AUTH_TOKEN with the
          # GitHub token which overrides OIDC. npm >=11.5.1 handles OIDC
          # and registry config automatically.

      - name: Upgrade npm for OIDC trusted publishing
        run: |
          # Node 24 ships npm 11.3.0 but OIDC requires >= 11.5.1
          npm install -g npm@latest
          echo "node: $(node --version)"
          echo "npm: $(npm --version)"
          echo "OIDC endpoint: ${ACTIONS_ID_TOKEN_REQUEST_URL:+available}"
          [ -n "${ACTIONS_ID_TOKEN_REQUEST_URL}" ] || { echo "ERROR: no OIDC endpoint"; exit 1; }

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Sync versions and prepare packages
        run: |
          VERSION=$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -1)
          echo "Publishing v$VERSION"

          # Update all package.json versions
          for dir in linux-x64-gnu linux-arm64-gnu darwin-x64 darwin-arm64; do
            # Copy built binary into npm dir
            cp artifacts/$dir/omz2cc npm/$dir/omz2cc
            chmod +x npm/$dir/omz2cc
            # Update version
            sed -i "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" npm/$dir/package.json
          done

          # Update main package version and dependency versions
          sed -i "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" npm/omz2cc/package.json
          for dir in linux-x64-gnu linux-arm64-gnu darwin-x64 darwin-arm64; do
            sed -i "s|\"@omz2cc/$dir\": \"[^\"]*\"|\"@omz2cc/$dir\": \"$VERSION\"|" npm/omz2cc/package.json
          done

      - name: Publish platform packages
        env:
          # setup-node sets NODE_AUTH_TOKEN to github.token even without
          # registry-url. This overrides OIDC. Clear it so npm uses OIDC.
          NODE_AUTH_TOKEN: ""
        run: |
          for dir in linux-x64-gnu linux-arm64-gnu darwin-x64 darwin-arm64; do
            echo "Publishing @omz2cc/$dir..."
            cd npm/$dir
            npm publish --access public --provenance
            cd ../..
          done

      - name: Publish main package
        env:
          NODE_AUTH_TOKEN: ""
        run: |
          cd npm/omz2cc
          npm publish --access public --provenance
