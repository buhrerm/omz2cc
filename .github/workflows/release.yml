name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  build:
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            npm-dir: linux-x64-gnu
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            npm-dir: linux-arm64-gnu
            use-cross: true
          - target: x86_64-apple-darwin
            os: macos-latest
            npm-dir: darwin-x64
          - target: aarch64-apple-darwin
            os: macos-latest
            npm-dir: darwin-arm64

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross
        if: matrix.use-cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build
        run: |
          if [ "${{ matrix.use-cross }}" = "true" ]; then
            cross build --release --target ${{ matrix.target }}
          else
            cargo build --release --target ${{ matrix.target }}
          fi

      - name: Copy binary to npm package
        run: |
          cp target/${{ matrix.target }}/release/omz2cc npm/${{ matrix.npm-dir }}/omz2cc
          chmod +x npm/${{ matrix.npm-dir }}/omz2cc

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.npm-dir }}
          path: npm/${{ matrix.npm-dir }}/

  publish:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Configure npm for OIDC trusted publishing
        run: |
          # Unset the GitHub token that setup-node injects — it's not an
          # npm token and prevents OIDC from being used.
          unset NODE_AUTH_TOKEN
          # Remove _authToken line from any .npmrc so npm falls back to OIDC
          find ~ -name .npmrc -exec sed -i '/_authToken/d' {} + 2>/dev/null || true
          sed -i '/_authToken/d' .npmrc 2>/dev/null || true
          echo "=== Debug: npm config ==="
          npm config list
          echo "=== Debug: .npmrc files ==="
          cat ~/.npmrc 2>/dev/null || echo "no ~/.npmrc"
          cat .npmrc 2>/dev/null || echo "no ./.npmrc"
          echo "=== Debug: npm version ==="
          npm --version
          echo "=== Debug: OIDC ==="
          if [ -n "${ACTIONS_ID_TOKEN_REQUEST_URL}" ]; then
            echo "OIDC endpoint: available"
          else
            echo "ERROR: OIDC endpoint not available"
            exit 1
          fi

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: Sync versions and prepare packages
        run: |
          VERSION=$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -1)
          echo "Publishing v$VERSION"

          # Update all package.json versions
          for dir in linux-x64-gnu linux-arm64-gnu darwin-x64 darwin-arm64; do
            # Copy built binary into npm dir
            cp artifacts/$dir/omz2cc npm/$dir/omz2cc
            chmod +x npm/$dir/omz2cc
            # Update version
            sed -i "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" npm/$dir/package.json
          done

          # Update main package version and dependency versions
          sed -i "s/\"version\": \".*\"/\"version\": \"$VERSION\"/" npm/omz2cc/package.json
          for dir in linux-x64-gnu linux-arm64-gnu darwin-x64 darwin-arm64; do
            sed -i "s|\"@omz2cc/$dir\": \"[^\"]*\"|\"@omz2cc/$dir\": \"$VERSION\"|" npm/omz2cc/package.json
          done

      - name: Publish platform packages
        env:
          NODE_AUTH_TOKEN: ""
        run: |
          for dir in linux-x64-gnu linux-arm64-gnu darwin-x64 darwin-arm64; do
            echo "Publishing @omz2cc/$dir..."
            cd npm/$dir
            npm publish --access public --provenance
            cd ../..
          done

      - name: Publish main package
        env:
          NODE_AUTH_TOKEN: ""
        run: |
          cd npm/omz2cc
          npm publish --access public --provenance
